node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Training/reference photos of real people — used offline to calibrate the
# classifiers. These must never be published: keep them out of any repo that
# backs a public deploy.
face types/
eye types/
nose types/
computer-mouse-single-click-betacut-hard-2-00-00.jpg

# Build artifacts that shouldn't be tracked
*.tsbuildinfo
vite.config.d.ts
vite.config.js
